{
  "name": "claude-subagents-lab",
  "version": "0.1.0",
  "private": true,
  "description": "Tiny starter project for the Claude Code subagents lab. Two source files, one empty test directory, no production dependencies.",
  "type": "module",
  "scripts": {
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=18"
  }
}
